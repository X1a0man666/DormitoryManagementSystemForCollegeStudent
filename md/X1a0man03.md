# X1a0man03.md —— 迭代三更新记录（全量数据：2024/2025 级学生与辅导员账号 + 宿舍排宿）

> 版本：迭代三 · 日期：2026-09-02
> 本次把首次运行自动生成的种子数据从"几行演示"改为**课程设定的全校全量数据**，
> 覆盖 2024、2025 两届所有学生与辅导员账号，并完成按学号顺序的宿舍排宿。

---

## 一、本次完成内容

1. **班级与性别结构（课程设定）**
   - 每学院 **4 专业** × 每专业 **4 班** × 每班 **40 人**。
   - 文科学院每班 **32 女 + 8 男**；理科学院每班 **8 女 + 32 男**；**女生学号在前**（序号小），男生在后。
   - 覆盖入学年份 **2024、2025** 两届 → **17 学院 × 4 × 4 × 40 × 2 = 21,760 名学生**。

2. **专业与分类**
   - 17 个学院各按南昌航空大学真实开设专业拟 4 个（`COLLEGE_MAJORS` 常量可整体改）。
   - 文科学院 6 个（09 经管 / 12 艺术 / 13 马克思 / 14 文法 / 15 航服音舞 / 16 外语），理科学院 11 个。
   - **软件学院（20）专业代码 1 = 软件工程** → 专业年级代码 `25201`（25级），辅导员账号 `counselor25201`。

3. **学号 / 账号**
   - 学号沿用 8 位 `StudentId` 规则：入学年后两位 + 学院代码 + 专业代码(1-4) + 班级代码(1-4) + 班内序号(01-40)。
   - **学生账号 = 学号**，密码 123456，姓名默认 = 学号。
   - **辅导员：每专业 × 每届 1 个**（17 × 4 × 2 = 136 个），账号 = `counselor`+专业年级代码，姓名默认 = 专业年级代码；仅建数据，审批作用域仍按学院。

4. **宿舍模型与排宿**
   - 每学院 **2 栋**：`<学院代码>A栋`=男、`<学院代码>B栋`=女（如 01A/01B、20A/20B）。
   - 每层 **45 间**（用户设定），每间 **4 人**；按学号顺序排宿（先女后男，序号小优先；2024 先于 2025）。
   - 楼栋层数依该性别两届总人数计算：多数性别楼 6 层、少数性别楼 2 层（17 学院共 34 栋、6,120 间房间；床位恰好覆盖 21,760 人）。

5. **改动文件**
   - `src/com/nchu/dorm/storage/DataCenter.java`：常量 + `seedColleges/seedStudents/seedCounselors/seedDormStaffAndAdmin/seedBuildingsAndRooms/seedAccounts`，删除旧演示 seed 逻辑。
   - `data/*.txt`：全部按新种子重新生成。
   - `src/com/nchu/dorm/ui/LoginView.java`：登录提示改演示账号。
   - `README.md`、`md/markdown.md`、`md/设计资料.md`：数据规模/账号规则/进度同步。

## 二、验证结果

- ✅ 编译（model/storage/util + 无 GUI 入口）通过。
- ✅ 生成后回读：colleges=17、buildings=34、rooms=6,120、students=21,760、counselors=136、accounts=21,898。
- ✅ 完整性断言：全部学号合法；全部学生入住；宿舍床位集合 == 学生集合（双向一致）；性别与楼栋（A 男/B 女）一致；学生账号 21,760 / 辅导员账号 136 / 无重复账号；样例 `25201101`(女)→20B栋、`25201140`(男)→20A栋，`counselor25201`/`counselor24201` 存在。
- ✅ 男 12,800 / 女 8,960 = 21,760（与文 6 / 理 11 分类吻合）。

## 三、已知限制 / 后续

- 全部学生已预排宿舍，因此"学生申请入住"演示在现数据下不可用（已入住）；转移/换宿等闭环待后续迭代。
- 辅导员账号 136 个但模型仍按学院作用域（无"专业"字段），如需按专业收窄审批需改 model/service（本次按用户选择未改）。
- 数据量较大（students.txt / accounts.txt 各约 2 万行），保存类操作每次全量写盘，规模可接受但偏重。
